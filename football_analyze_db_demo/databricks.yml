# This is a Databricks asset bundle definition for football_analyze_db_demo.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: football_analyze_db_demo
  uuid: da4f4824-a4af-428b-887d-38f6d27da1ba

include:
  - resources/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-2959402884272406.6.azuredatabricks.net

  prod:
    mode: production
    workspace:
      host: https://adb-2959402884272406.6.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/rayz.le0115@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/rayz.le0115@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: rayz.le0115@gmail.com
        level: CAN_MANAGE
